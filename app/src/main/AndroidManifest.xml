<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.wanusa">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/apk"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".DashboardActivity" android:theme="@style/Theme.AppCompat.NoActionBar" android:parentActivityName=".DashboardActivity"/>
        <activity android:name=".QuestionActivity" android:theme="@style/Theme.AppCompat.NoActionBar" android:parentActivityName=".DashboardActivity"/>
        <activity android:name=".Hasil_Kuis" />
        <activity android:name=".Soal" />
        <activity android:name=".Start" />
        <activity android:name=".Ragam" />
        <activity android:name=".Sejarah" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>